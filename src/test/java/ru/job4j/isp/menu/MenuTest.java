package ru.job4j.isp.menu;

import org.junit.Test;
import ru.job4j.ood.isp.menu.two.Menu;

import static org.hamcrest.Matchers.is;
import static org.junit.Assert.*;
import static ru.job4j.ood.isp.menu.two.Print.NEW_LINE;

public class MenuTest {
/*
    private final Menu tree = new Menu("1", new ActionSelect());

    @Test
    public void testFindBy() {
        tree.add("1", "2", new ActionSelect());
        tree.add("1", "3", new ActionSelect());
        tree.add("1", "4", new ActionSelect());
        tree.add("4", "5", new ActionSelect());
        tree.add("5", "6", new ActionSelect());
        assertNotNull(tree.findBy("5"));
    }

    @Test
    public void testAddTrue() {
        assertTrue(
                tree.add("1", "2", new ActionSelect())
        );
    }

    @Test
    public void testAddTrueTwo() {
        tree.add("1", "2", new ActionSelect());
        assertTrue(tree.add("2", "3", new ActionSelect()));
    }

    @Test
    public void testAddFalse() {
        assertFalse(tree.add("2", "3", new ActionSelect()));
    }

    @Test
    public void testPrint() {
        tree.add("1", "2", new ActionSelect());
        tree.add("1", "3", new ActionSelect());
        tree.add("1", "4", new ActionSelect());
        tree.add("4", "5", new ActionSelect());
        tree.add("5", "6", new ActionSelect());
        assertThat(
                tree.print(),
                is("Element{str='1'}" + NEW_LINE
                        + "Element{str='2'}" + NEW_LINE
                        + "Element{str='3'}" + NEW_LINE
                        + "Element{str='4'}" + NEW_LINE
                        + "Element{str='5'}" + NEW_LINE
                        + "Element{str='6'}" + NEW_LINE)
        );
    }

 */
}